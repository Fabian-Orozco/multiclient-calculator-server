	#def __configRouter(self, id="-"):
		# printMsgTime(f"{TXT_RED}Testing{TXT_RESET} configuring router")
		# self.__node = Router(host, port, id) 
		# self.__node.loadNeighbords(sys.argv[])
		# tries to connect with server =========================== this goes to the router class
		# while(True):
		# 	try:
		# 		self.__routerConnection.connect((self.__serverHost, self.__serverPort))
		# 		break
		# 	except KeyboardInterrupt:
		# 		self.shutDownServer()
		# 	except:
		# 		printErrors("Server not found. Retrying connection.")
		# 		sleep(5)
		# self.__sendMsg(self.__routerConnection, "{\"type\":\"router\",\"id\":\"A\"}")